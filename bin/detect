#!/bin/sh


if [ -f $1/build.gradle ]; then
  echo "Gradle" 
  // TODO: Parse file for android attributes
  exit 0
else
  (>&2 echo "Could not find a 'build.gradle' file! Please check that it exists and is commited to Git.")
  exit 1
fi
