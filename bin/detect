#!/bin/sh

# This pack is valid for apps with an AndroidManifest.xml
# TODO: Make this more intelligent, parse build.gradle?
if [[ -n $(find . -name  AndroidManifest.xml) ]]; then
  echo "Android Framework"
  exit 0
else
  exit 1
fi
